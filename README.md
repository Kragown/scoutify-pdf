## Getting Started

First, run the development server:

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

## Test

Second, test api:

```bash
npm run test:api
```

You may also see curl commands in examples/README.md

There is also a swagger.yaml
